[project]
name = "tracing-overhead"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.7",
    "numpy>=2.3.3",
    "click>=8.1.0",
    "ruff>=0.14.0",
]

[project.scripts]
tracing-overhead = "main:cli"
